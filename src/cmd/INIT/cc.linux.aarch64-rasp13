: linux.aarch64-rasp13 cc wrapper : 2006-02-14 :

HOSTTYPE=linux.aarch64-rasp13

case " $* " in
*" -dumpmachine "*) echo $HOSTTYPE; exit ;;
esac

/usr/bin/cc -Wno-implicit-int -Wno-implicit-function-declaration -P -g "$@" -lm
